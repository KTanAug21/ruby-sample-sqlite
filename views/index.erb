<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo App</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }

    .container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 600px;
      padding: 40px;
      margin-top: 40px;
    }

    h1 {
      color: #333;
      margin-bottom: 30px;
      font-size: 2.5em;
      text-align: center;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .add-task-form {
      margin-bottom: 30px;
      display: flex;
      gap: 10px;
    }

    .add-task-form input {
      flex: 1;
      padding: 15px 20px;
      border: 2px solid #e0e0e0;
      border-radius: 10px;
      font-size: 16px;
      outline: none;
      transition: border-color 0.3s;
    }

    .add-task-form input:focus {
      border-color: #667eea;
    }

    .add-task-form button {
      padding: 15px 30px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 10px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .add-task-form button:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .add-task-form button:active {
      transform: translateY(0);
    }

    .tasks-list {
      list-style: none;
    }

    .task-item {
      background: #f8f9fa;
      border-radius: 12px;
      padding: 20px;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 15px;
      transition: transform 0.2s, box-shadow 0.2s;
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .task-item:hover {
      transform: translateX(5px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .task-item.completed {
      opacity: 0.6;
    }

    .task-item.completed .task-title {
      text-decoration: line-through;
      color: #999;
    }

    .task-checkbox {
      width: 24px;
      height: 24px;
      cursor: pointer;
      accent-color: #667eea;
    }

    .task-content {
      flex: 1;
    }

    .task-title {
      font-size: 18px;
      color: #333;
      margin-bottom: 5px;
      font-weight: 500;
    }

    .task-date {
      font-size: 12px;
      color: #999;
    }

    .task-actions {
      display: flex;
      gap: 10px;
    }

    .task-button {
      padding: 8px 16px;
      border: none;
      border-radius: 8px;
      font-size: 14px;
      cursor: pointer;
      transition: all 0.2s;
      font-weight: 500;
    }

    .task-button.complete {
      background: #10b981;
      color: white;
    }

    .task-button.complete:hover {
      background: #059669;
      transform: translateY(-1px);
    }

    .task-button.incomplete {
      background: #f59e0b;
      color: white;
    }

    .task-button.incomplete:hover {
      background: #d97706;
      transform: translateY(-1px);
    }

    .task-button.delete {
      background: #ef4444;
      color: white;
    }

    .task-button.delete:hover {
      background: #dc2626;
      transform: translateY(-1px);
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #999;
    }

    .empty-state-icon {
      font-size: 64px;
      margin-bottom: 20px;
    }

    .empty-state-text {
      font-size: 18px;
    }

    form {
      display: inline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📝 Todo App</h1>

    <form class="add-task-form" action="/add" method="post">
      <input type="text" name="title" placeholder="Add a new task..." required autofocus>
      <button type="submit">Add Task</button>
    </form>

    <% if @tasks.empty? %>
      <div class="empty-state">
        <div class="empty-state-icon">✨</div>
        <div class="empty-state-text">No tasks yet. Add one above to get started!</div>
      </div>
    <% else %>
      <ul class="tasks-list">
        <% @tasks.each do |task| %>
          <li class="task-item <%= 'completed' if task['completed'].to_i == 1 %>">
            <input type="checkbox" class="task-checkbox" 
                   <%= 'checked' if task['completed'].to_i == 1 %>
                   onchange="this.closest('li').querySelector('form[action*=<%= task['completed'].to_i == 1 ? 'undone' : 'done' %>]').submit()">
            <div class="task-content">
              <div class="task-title"><%= task['title'] %></div>
              <div class="task-date">Created <%= task['created_at'] %></div>
            </div>
            <div class="task-actions">
              <% if task['completed'].to_i == 1 %>
                <form action="/undone/<%= task['id'] %>" method="post">
                  <button type="submit" class="task-button incomplete">Undo</button>
                </form>
              <% else %>
                <form action="/done/<%= task['id'] %>" method="post">
                  <button type="submit" class="task-button complete">Done</button>
                </form>
              <% end %>
              <form action="/delete/<%= task['id'] %>" method="post" 
                    onsubmit="return confirm('Are you sure you want to delete this task?');">
                <button type="submit" class="task-button delete">Delete</button>
              </form>
            </div>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
</body>
</html>

